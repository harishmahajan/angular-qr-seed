{
  "name": "angular-simple-seed",
  "version": "0.0.0",
  "description": "a angular-simple-seed project",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.8.8",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  },
  "dependencies": {
    "angular-qrcode": "^7.2.0"
  }
}
